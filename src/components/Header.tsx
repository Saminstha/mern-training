import { Typography } from "@mui/material";
function Header(){
    return(
        <Typography variant="h1" component="h2">
        Course Students
        </Typography>

    )
}

export default Header;
